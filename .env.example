# Optional: bake a TMDB key into the build instead of entering it in Settings.
# Note: any key in a client-side build is visible to whoever can open the site,
# so only do this for a private/personal deployment.
VITE_TMDB_API_KEY=
